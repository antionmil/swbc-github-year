node_modules
.next
# Every env file, however it is named — `.env` and `.env.local` alone missed
# `.env.production` and `.env.*.local`, which is exactly the kind of file that
# gets created in a hurry and committed by accident.
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts
/public/artifacts/*
!/public/artifacts/.gitkeep
.next-build
